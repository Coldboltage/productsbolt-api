{
  "scripts": {
    "deploy:worker": "docker pull coldbolt/productsbolt-worker:latest && docker service update --force productsbolt_worker && docker service update --force productsbolt_headful-worker"
  }
}
